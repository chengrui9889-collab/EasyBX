# 项目初始化记录

**执行时间**: 2026-05-15
**执行人**: AI Assistant (Role: DevOps)

## 1. 检查项
- [x] `specs/产品概述.md` 存在
- [x] `specs/技术栈.md` 存在
- [x] `specs/项目结构.md` 存在
- [x] `specs/开发规范.md` 存在
- [x] `specs/开发路线图.md` 存在
- [x] 目标目录为空，安全可初始化

## 2. 执行日志

### 2.1 核心初始化
- **Git 初始化**: `git init` — 成功
- **关键工具检测**:
  - Git 2.54.0 ✓
  - Node.js 24.11.1 ✓
  - npm 11.6.2 ✓
  - Python 3.13.7 ✓

### 2.2 目录创建
- [x] 创建 `server/` 及其子目录 (`app/`, `app/models/`, `app/schemas/`, `app/api/`, `app/services/`, `app/utils/`, `tests/`, `alembic/versions/`)
- [x] 创建 `web/` 及其子目录 (`src/api/`, `src/stores/`, `src/types/`, `src/hooks/`, `src/layouts/`, `src/pages/`, `src/components/ui/`, `src/components/invoices/`, `src/components/batches/`, `src/components/exports/`, `src/lib/`, `public/`)
- [x] 创建 `data/`
- [x] 创建 `uploads/`
- [x] 创建 `docs/开发记录/`

### 2.3 配置文件
- [x] `.gitignore` — 忽略 data、uploads、node_modules、__pycache__、.env 等
- [x] `.env.example` — 包含 DATABASE_URL、JWT_SECRET、SERVER_PORT 等 8 个变量
- [x] `.env` — 从 .env.example 复制
- [x] `README.md` — 项目简介和启动指南
- [x] `start.bat` — Windows 一键启动脚本

### 2.4 后端 (Python FastAPI)
- [x] `server/requirements.txt` — Python 依赖清单（已通过 pip freeze 生成）
- [x] `server/pyproject.toml` — Ruff + mypy 配置
- [x] `server/main.py` — FastAPI 应用入口，挂载路由 /api + CORS
- [x] `server/app/config.py` — Settings 类，读取 .env
- [x] `server/app/database.py` — SQLAlchemy 引擎 + Session + Base + get_db
- [x] `server/app/dependencies.py` — JWT 验证 + get_current_user 依赖注入
- [x] `server/app/api/router.py` — 主路由聚合 (auth/invoices/batches/exports)
- [x] `server/app/api/auth.py` — POST /auth/register, /auth/login
- [x] `server/app/api/invoices.py` — CRUD /invoices
- [x] `server/app/api/batches.py` — CRUD /batches + 发票关联 + Excel 导出
- [x] `server/app/api/exports.py` — PDF 导出 + 报销单预览
- [x] `server/app/services/auth_service.py` — 注册/登录/密码哈希/JWT 生成（已实现）
- [x] `server/app/services/ocr_service.py` — 存根
- [x] `server/app/services/invoice_service.py` — 存根
- [x] `server/app/services/batch_service.py` — 存根
- [x] `server/app/services/excel_service.py` — 存根
- [x] `server/app/services/pdf_service.py` — 存根
- [x] `server/app/services/reimbursement_service.py` — 存根
- [x] `server/app/utils/file_utils.py` — 文件路径生成 + 扩展名校验
- [x] `server/app/utils/image_utils.py` — 图片旋转正置
- [x] `server/alembic.ini` — Alembic 配置
- [x] `server/alembic/env.py` — 迁移环境（已修复 sys.path）
- [x] `server/alembic/script.py.mako` — Mako 迁移模板

### 2.5 数据库模型
- [x] `User` — id, username, password_hash, display_name, created_at
- [x] `Invoice` — id, user_id, invoice_no, amount, date, category, vendor, file_path, status, ocr_raw_data
- [x] `ReimbursementBatch` — id, user_id, department, period_start/end, reporter, reviewer, payee, bank_account, bank_name, total_amount, status
- [x] `BatchInvoice` — id, batch_id, invoice_id, expense_item, remark, is_substitute, substitute_for
- [x] `Archive` — id, batch_id, archive_path, created_at

### 2.6 Pydantic Schema
- [x] `User` — RegisterRequest, LoginRequest, TokenResponse, UserResponse
- [x] `Invoice` — InvoiceResponse, UpdateInvoiceRequest
- [x] `Batch` — CreateBatchRequest, UpdateBatchRequest, BatchResponse, BatchDetailResponse, BatchInvoiceResponse, BatchInvoiceRequest
- [x] `Export` — PdfExportRequest, ReimbursementItem, ReimbursementPreviewResponse

### 2.7 Alembic 迁移
- [x] `alembic revision --autogenerate -m "init"` — 成功生成迁移文件
- [x] `alembic upgrade head` — 成功创建 5 张数据库表

### 2.8 前端 (React + TypeScript + Vite)
- [x] `package.json` — 含 11 个 dependencies + 11 个 devDependencies
- [x] `tsconfig.json` — strict 模式 + path alias `@/*`
- [x] `vite.config.ts` — React 插件 + /api 代理到 localhost:0220
- [x] `tailwind.config.js` — Tailwind CSS 配置
- [x] `postcss.config.js` — PostCSS 配置
- [x] `index.html` — 入口 HTML
- [x] `src/main.tsx` — React 入口 + BrowserRouter
- [x] `src/App.tsx` — 路由配置 (7 条路由 + 路由守卫)
- [x] `src/index.css` — Tailwind 指令
- [x] `src/lib/utils.ts` — cn() + formatCurrency + formatDate
- [x] `src/lib/constants.ts` — 状态/分类/布局常量
- [x] `src/api/client.ts` — axios 实例 + JWT 拦截器
- [x] `src/api/auth.ts` — 登录/注册 API
- [x] `src/api/invoices.ts` — 发票 CRUD API
- [x] `src/api/batches.ts` — 批次 CRUD API
- [x] `src/api/exports.ts` — 导出 API
- [x] `src/stores/authStore.ts` — Zustand 认证状态
- [x] `src/stores/invoiceStore.ts` — Zustand 发票状态
- [x] `src/stores/batchStore.ts` — Zustand 批次状态
- [x] `src/types/user.ts` — 用户类型
- [x] `src/types/invoice.ts` — 发票类型
- [x] `src/types/batch.ts` — 批次类型
- [x] `src/types/export.ts` — 导出类型
- [x] `src/layouts/AppLayout.tsx` — 主布局 (侧边栏 + 顶栏 + 内容区)
- [x] `src/components/ui/Button.tsx` — 按钮组件 (4 variants + 3 sizes)
- [x] `src/components/ui/Input.tsx` — 输入框组件 (含 label + error)
- [x] `src/components/ui/Sidebar.tsx` — 侧边栏导航 (概览/文件/报销单)
- [x] `src/pages/LoginPage.tsx` — 登录页（已完整实现）
- [x] `src/pages/RegisterPage.tsx` — 注册页（已完整实现）
- [x] `src/pages/DashboardPage.tsx` — 概览页（入口链接）
- [x] `src/pages/InvoicesPage.tsx` — 发票管理页（存根）
- [x] `src/pages/BatchesPage.tsx` — 批次列表页（存根）
- [x] `src/pages/BatchDetailPage.tsx` — 批次详情页（存根）
- [x] `src/pages/ReimbursementPreviewPage.tsx` — 报销单预览页（存根）

### 2.9 验证结果
- [x] **Python 导入检查**: `python -c "from main import app"` — Import OK
- [x] **所有路由已注册**: 共 21 条路由（含 1 条 health + 20 条业务路由）
- [x] **API 健康检查**: `GET /api/health` → `{"status":"ok","service":"EasyBX"}`
- [x] **数据库迁移**: 5 张表全部创建成功
- [x] **TypeScript 类型检查**: `npx tsc --noEmit` — 0 错误
- [x] **npm 依赖安装**: 22 个包安装成功

## 3. 遗留问题 / TODO
- [ ] PaddleOCR 和 PaddlePaddle 未安装（体积大，按需安装：`pip install paddleocr paddlepaddle`）
- [ ] 需要用户确认 `.env` 配置（特别是 JWT_SECRET 的值）
- [ ] `start.bat` 需要实际测试（需确保 cmd 路径正确）

## 4. 下一步建议
请按照 `specs/开发路线图.md` 进入 **Phase 1: MVP — 核心报销闭环**：

- **Phase 1.1**: 认证后端 — 将 auth_service.py 连接到 auth.py 路由
- **Phase 1.2**: 认证前端 — 完善登录/注册表单校验和错误提示
- **Phase 1.3**: 发票后端 — 实现文件上传、存储、OCR 调用
- **Phase 1.4**: 发票前端 — 上传区、识别结果卡片、编辑表单、发票列表
